# Copy to a local file and replace every <...> path. Do not commit local paths.
experiment_id: onomarg_supervised_seed20
seed: 20
annotation_csv: <PATH_TO_FIRAT_ANNOTATION_CSV>
audio_root: <PATH_TO_FIRAT_AUDIO_ROOT>
htsat_checkpoint: <PATH_TO_HTSAT_AUDIOSET_CHECKPOINT>
htsat_sha256: <SHA256_OF_HTSAT_CHECKPOINT>
bart_model_id: facebook/bart-base
bart_revision: aadd2ab0ae0c8268c7c9693540e9904811f36177
sample_rate: 32000
n_fft: 1024
hop_length: 320
f_min: 50
f_max: 14000
n_mels: 64
max_audio_sec: 10
batch_size: 16
num_workers: 8
epochs: 20
learning_rate: 3.0e-5
weight_decay: 1.0e-6
warmup_fraction: 0.05
grad_clip: 2.0
label_smoothing: 0.1
max_length: 30
min_length: 2
num_beams: 3
